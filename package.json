{
  "name": "esbuild-react-example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@craftamap/esbuild-plugin-html": "^0.2.2",
    "esbuild": "^0.14.21"
  },
  "dependencies": {
    "core-js": "3.21.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "node build.js",
    "start": "yarn build && cd ./dist && http-server"
  }
}
